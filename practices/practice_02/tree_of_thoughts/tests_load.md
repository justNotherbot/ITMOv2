# Нагрузочные проверки

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| P95 при 256 КБ diff | 5 RPS, 10 мин; POST /api/reviews; JSON тело с `diff` длиной 262144±5% байт | p95 < 1.0 с; p99 < 1.5 с; error rate < 1%; 0% 5xx | HTTP latency p50/p95/p99 (мс); распределение кодов (2xx/4xx/5xx); фактический RPS | Отчёт vegeta/hey (перцентили и коды); Uvicorn access logs со статусом и временем ответа |
| Лимит размера | 1 RPS, 3 мин; серии с `diff` = 300 КБ и 1 МБ; контрольная серия с 256 КБ | >256 КБ → 100% 413; ≤256 КБ → 2xx; 0% 5xx | Соответствие кодов ответов размеру тела (413 для >256 КБ, 2xx для ≤256 КБ); доля 5xx=0% | Журналы HTTP и ответы сервера; отчёт vegeta/hey с распределением кодов |
| Стабильность таймаута LLM | 1 RPS, 5 мин; инъекция задержки LLM 12 с (мок/заглушка) | 100% 503/504 за ≤11 с; 0% 200 | Долю 503/504; время до ошибки (TTFB и total); отсутствие успешных ответов | Отчёт vegeta/hey (время и коды); Uvicorn error/access logs с маппингом ошибок LLM в 503/504 |

Если нагрузочное тестирование пока не требуется, обоснуйте это и назовите условие, после которого оно понадобится.

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-03 (см. prompt_03.txt).
- Что проверили и исправили сами: синхронизировали пороги и сценарии с `context/problem.md`; заменили абстракции на p95/p99, коды 413/503/504 и источники Evidence (vegeta/hey + Uvicorn logs).
